# R157 / F09–F10 — Experiment disposition and finance default freeze

## Decision

PASS. F08 produced zero qualified paid experiment candidates, so F09 started
and completed zero experiments at CNY 0. This is the preregistered decision
rule operating as intended: no underpowered run was substituted for missing
causal and sample-size evidence.

F10 freezes all nine capabilities as permanent opt-in. The graduation registry
has zero pending entries; Settings keeps all nine defaults false; the manifest
keeps all nine classified `content_affecting`; README, evaluation documentation
and the complete 26-flag machine snapshot agree. No capability was graduated
from fixture scores or H2 mechanism evidence.

The permanent guard rejects an experiment started without an F08 candidate,
any drift between the frozen snapshot and Settings, a true default for an
opt-in capability, a manifest classification drift, a disposition mismatch, or
stale README/evaluation statements. Both preserved mutation commands exit 1.

No paid provider call, full cohort, golden truth, scoring contract, product
threshold, default flag, or remote state changed. Round cost is CNY 0.

The final local gate passed with 1,221 tests, 7 registered skips, 60/60 guards
wired, and tracked files unchanged.
