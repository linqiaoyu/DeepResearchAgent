# 019-B/C 付费验证预登记草案

状态：**DRAFT，未经 PM 明确确认不得生效或支出。**

实验设计按 A10 机械落定为 **丙：双臂影子录制**。题集冻结为 Q01、Q04、Q16、Q19、Q26、Q28；若 PM 要替换题目，必须在首笔支出前重写并重新确认本文件。候选注入和真实模式离线 replay 两项零成本工单必须先全绿。

## 假设分层

机制层假设回答“调用、录制、隔离、盲化、回放和交付链是否按契约工作”，达到预登记阈值可以点亮相应机制。质量层假设回答“LLM 洞察候选是否比确定性候选更有用且无害”，本轮六题只能提供否决证据，不能证明普遍优越。

**小样本下质量层证据只能作为否决条件，不能作为点亮的充分条件。**

## 支出登记

### 支出 1：真实 provider/tool-contract 预飞行

- 预算上限：¥2.00。
- 支出内容：仅 Q01 的单个受控真实检索/结构化 provider 调用和一次 deepseek-v4-flash 最小结构化调用；不得生成全报告。
- 机制层可证伪假设 M1：真实响应能通过 ToolSpec/统一 LLMClient，timeout/retry、tokens、cost、latency 同时写入 ledger 与 trajectory，且审计产物无密钥。
- 测量：逐调用比对 ledger/trajectory；调用数必须精确等于预登记数；缺失记录数 0；密钥扫描命中 0；实际成本 ≤ ¥2。
- 条件 A → 点亮：100% 调用双写、结构化解析成功、密钥命中 0、无预算异常，点亮“真实 provider 边界可录制”，才允许支出 2。
- 条件 B → 回滚：任一调用漏记、越过 ToolSpec、泄密、解析失败或实际成本超过单项上限，立即停机并回滚 provider 接线，余额不得转用。
- 两者都不满足：例如 provider 可用但成本字段无法与官方 usage 对账，停止并交 PM，不得扩大样本。
- 回滚触发：provider schema 漂移、重复 retry 造成非预登记调用、单次实际成本 > 预估 2 倍、任何 secret 出现在文件或异常文本。

### 支出 2：六题双臂影子超集录制

- 预算上限：¥10.00。
- 支出内容：Q01、Q04、Q16、Q19、Q26、Q28；每题在同一重规划分叉点产生“确定性候选”和“LLM 洞察候选”，两臂检索调用均录制，只有预先指定主臂进入在线报告。
- 机制层可证伪假设 M2：六题均产生两臂、两臂查询与 tool calls 可区分、Evidence 不串臂，并能在零网络环境分别严格回放到逐字相同的臂产物。
- 质量层可证伪假设 Q2：LLM 洞察臂不会在 groundedness、specificity、harm 中任一项产生明确劣化；若不能增加有用证据也属于否决信号，但“看起来更好”不能点亮功能。
- 测量：每题候选数=2；臂 ID 完整率=100%；跨臂 Evidence 污染数=0；strict replay cache miss=0；报告逐字匹配率=100%；记录唯一来源数、无效来源数、检索调用数和分支成本。
- 条件 A → 点亮：M2 全部机械指标达标，点亮“丙方案录制与回放机制”；Q2 仅记为非否决观察，继续支出 3。
- 条件 B → 回滚：任一题缺臂、串臂、cache miss、报告不一致，或 LLM 臂出现无来源关键结论/明显有害建议，回滚候选选择器并保留 deterministic 主路径。
- 两者都不满足：例如五题通过、一题因公开来源暂时不可达而无法归因，停止并交 PM，不用新增题目补齐结果。
- 回滚触发：任何默认开关被改成 true；任何实验臂 Evidence 进入控制臂；任一题成本超该题预估 2 倍；总成本累计超过 ¥12（含支出 1）。

### 支出 3：qwen3.7-plus 盲化 judge 与 citation_support

- 预算上限：¥10.00。
- 支出内容：只评估支出 2 已录制的 12 个臂产物；输入先经实验条件 redaction，不做新检索。
- 机制层可证伪假设 M3：judge 看不到臂名、Reflector、开关或 treatment/control 标识，所有评分响应可解析、可对账、可重放。
- 质量层可证伪假设 Q3：LLM 洞察臂相对确定性臂在 groundedness、specificity、harm、efficiency、replayability、usefulness 上没有触发预登记否决；质量结果只可否决，不可单独点亮。
- 测量：盲化词表命中 0；有效 judge 样本 12/12；citation_support 的引用闭合率 100%；逐题记录六维配对差值、调用成本和解析失败。
- 条件 A → 点亮：M3 的盲化、解析、对账、重放全部 100% 达标，点亮“盲评机制”；Q3 只允许结论为“未触发否决”，不得写成“LLM 臂已优于控制”。
- 条件 B → 回滚：任一条件词泄漏、引用闭合低于 100%、超过 1 个解析失败，或 LLM 臂在任一题出现 harm 上升/groundedness 明确下降，回滚该内容开关并保留 dark。
- 两者都不满足：评分分歧大且无预登记多数规则可裁决时，停止交 PM，不追加 judge 样本。
- 回滚触发：blind redaction 失败、judge 输入包含决策记录、单次成本 > 预估 2 倍、累计成本超过 ¥22。

### 支出 4：精确 cache-miss 补录预备金

- 预算上限：¥3.00；只有支出 2/3 出现已定位到具体 role/tool/key 的 cache miss 时可启用，否则支出为 0。
- 机制层可证伪假设 M4：对明确缺失键做一次定向补录即可消除 cache miss，且不改变既有键或产物语义。
- 测量：补录前后缺失键清单；只允许调用清单中的精确项；补录后 strict replay cache miss 必须为 0。
- 条件 A → 点亮：一次定向补录后缺失数从 N 降到 0，记录合并不改原有条目，接受补录轨迹。
- 条件 B → 回滚：仍有 miss、出现新 miss、需要放宽 strict 键或需要第二轮补录，停止并回滚合并结果。
- 两者都不满足：provider 暂时错误导致无法判断键是否正确，停止交 PM。
- 回滚触发：补录调用不在缺失清单、补录改变已有响应、累计成本将超过 ¥25。

## 三层硬熔断

1. 单项熔断：各支出项不得超过自身上限；余额不得跨项挪用。代码层每次调用必须传预估成本，实际成本大于预估 2 倍立即抛错中断。
2. 全轮熔断：019-B/C provider 实际累计成本上限固定为 **¥25.00**；达到上限前若下一次调用可能越界，不得发起。
3. 意外支出熔断：发现未预登记 role、tool、题目、retry、网络端点或供应商账单与 ledger 无法对账，立即停止全部外部调用并交 PM。

预算算术：¥2 + ¥10 + ¥10 + ¥3 = **¥25**。任何未列出的“顺便重跑”、全量 G4 或扩大 judge 样本均无预算授权。

## 付费前置闸

- PM 已明确确认本草案及六题题集。
- `WO-019B-01` 与 `WO-019B-02` 在 A8 llm stub 轨迹上全绿，严格回放零网络。
- A0 全套闸门仍绿，manifest 比较无未知 flag。
- 所有 11 个 content-affecting 开关默认值仍为 false；仅运行环境覆盖。
- 运行目录受 `.gitignore` 覆盖，公开产物只含哈希、≤1,000 字符摘录和结构化抽取。
- 每次支出前在正式 `preregistration.md` 写入预计调用数、预计单次成本和剩余预算；本草案本身不构成支出许可。

## 真实研究包验收与处置

支出后必须按 `deliverable_checklist.md` 原阈值验收。若真实报告复现 A8 已暴露的“关键发现与分析逐字重复”或“人民币科学计数法直出”，属于结构性质量否决：内容开关保持 dark，并在 README 真实模式边界中披露，不得通过修改验收清单掩盖。只有机制层全部通过、无质量否决、总成本可追溯且 PM 复核后，才讨论下一轮是否点亮；本小样本本身不授权修改任何默认值。

## 官方计价基线（获取日 2026-07-25）

- DeepSeek V4 Flash：中国区官方价格页列出每百万 tokens 缓存命中输入 ¥0.02、缓存未命中输入 ¥1、输出 ¥2：`https://api-docs.deepseek.com/zh-cn/quick_start/pricing/`。
- 阿里云百炼 qwen3.7-plus 北京区固定版本在输入 ≤256K 时每百万 tokens 输入 ¥2、输出 ¥8；256K–1M 时输入 ¥6、输出 ¥24：`https://help.aliyun.com/zh/model-studio/model-pricing`。
- 百炼隐式上下文缓存命中按输入价格 20% 计，本项目未启用的显式缓存不计入预估：`https://help.aliyun.com/zh/model-studio/context-cache`。
- 供应商最终账单为权威值；本项目 ledger 是基于 usage 的护栏估算，不得表述为发票。
